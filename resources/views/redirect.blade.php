<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="refresh" content="0; url={{ $redirect }}">
    <title>Redirecting to {{ $redirect }}</title>
</head>
<body>
    <h1>Redirecting to <a href="{{ $redirect }}" title="{{ $redirect }}" rel="noreferrer nofollow">{{ $redirect }}</a></h1>
</body>
</html>